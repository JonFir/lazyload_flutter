import 'package:flutter/material.dart';

class NewsWidgetLeaderboards extends StatelessWidget {
  const NewsWidgetLeaderboards({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
